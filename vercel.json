{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",      // Or the correct path if your frontend is in a folder
      "use": "@vercel/react"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"  // For single-page apps
    }
  ]
}
